# Server
PORT=8082
LOG_LEVEL=info

# CORS — comma-separated origins (use * for development)
CORS_ORIGINS=*

# API Keys — comma-separated (leave empty to disable auth in dev)
SEO_ADS_API_KEYS=

# Rate limiting
RATE_LIMIT_RPM=120

# ── AI Agent — Smart LLM Routing ─────────────────────────
# Both keys configured = automatic routing (Gemini default, Claude for complex tasks)
# Only one key = uses that provider for everything

# Gemini (PRIMARY — default for content, SEO, hashtags, campaigns)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
GEMINI_PRO_MODEL=gemini-2.5-pro-preview-06-05

# Claude (SECONDARY — for orchestration, cultural adaptation, synthesis)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
