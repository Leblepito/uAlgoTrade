# Backend configuration
#
# Required:
# - Postgres (auth/subscription tables live here)
# - Auth signing key (JWT HMAC)

# Either set ConnectionStrings:Postgres in appsettings or use this env var:
POSTGRES_CONNECTION_STRING=Host=localhost;Port=5432;Database=finance;Username=postgres;Password=postgres

# Railway/managed Postgres often provides a URL-style connection string (supported):
# DATABASE_URL=postgresql://user:pass@host:5432/db?sslmode=require

# JWT signing key (min 32 chars recommended)
Auth__SigningKey=dev_signing_key_change_me_please_32chars_min

# Optional: restrict CORS for cookie-based refresh (recommended once you have a domain)
# Cors__AllowedOrigins__0=http://localhost:3000

# Telegram signal delivery (optional)
# Put your bot token here, e.g. 123456:ABC...
# Telegram__BotToken=
# Telegram__ChatId=@your_channel_username_or_chat_id
# Telegram__JoinUrl=https://t.me/ukeytr
#
# Alternative env names supported by backend:
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
